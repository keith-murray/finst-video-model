[project]
name = "finst-video-model"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Keith Murray", email = "murraykeith64@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
finst-video-model = "finst_video_model:main"

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"
